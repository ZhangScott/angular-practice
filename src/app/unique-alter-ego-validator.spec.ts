import { UniqueAlterEgoValidator } from './unique-alter-ego-validator';

describe('UniqueAlterEgoValidator', () => {
  it('should create an instance', () => {
    expect(new UniqueAlterEgoValidator()).toBeTruthy();
  });
});
